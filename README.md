# multimodal-ai-data-copilot
A multimodal AI-powered data assistant that analyzes text, images, and transcribes audio (meetings) to generate business insights and support decision-making using the OpenAI API.

After cloning the repo please run -> pip install -r requirements.txt to install the necessary dependencies
